import styled from 'styled-components';

const StyledDateLine = styled.p`
  time {
    font-weight: 300;
  }

  svg {
    height: 12px;
    width: 12px;
    margin-left: 5px;
  }

  hr {
    margin: 15px 0;
  }
`

export default StyledDateLine;