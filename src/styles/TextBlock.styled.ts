import styled from 'styled-components';

const StyledTextBlock = styled.p`
  font-size: 16px;
  line-height: 1.65;
  font-weight: 300;
  margin-bottom: 30px;

  strong {
    font-weight: 900;
  }
`

export default StyledTextBlock;